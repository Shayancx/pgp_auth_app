<div class="success-icon">🔓</div>

<% if flash['notice'] %>
  <div class="notice"><%= flash['notice'] %></div>
<% end %>

<div class="user-greeting">Welcome, <%= CGI.escapeHTML(@account[:username]) %>!</div>

<h2>Secure Dashboard</h2>

<div class="welcome-container">
  <p>You've successfully authenticated using cryptographic verification.</p>
  
  <div class="security-info">
    <strong>✓ Account Verified:</strong> PGP key authentication<br>
    <strong>✓ Enhanced Security:</strong> 
    <% if @account[:pgp_only_mode] %>
      PGP-only mode active
    <% else %>
      Password + PGP 2FA enabled
    <% end %><br>
    <strong>✓ Zero-Knowledge:</strong> Server stores no secrets<br>
    <% if @account[:failed_password_count] > 0 %>
      <strong>⚠ Password Failures:</strong> <%= @account[:failed_password_count] %>/10 before PGP-only mode
    <% end %>
  </div>
  
  <p>Your account is protected by industry-standard security measures with progressive security escalation.</p>
</div>

<a href="/logout" class="logout-btn">Sign Out</a>
